# Vernier

Vernier is a dev-time Vite overlay for measuring UI problems and exporting agent-readable feedback sessions.

Current status: M0 scaffold.

## Development

```bash
npm install
npm run verify:m0
cd examples/react-vite
npm run dev
```

In dev mode, the browser console should show `[vernier] active`.

